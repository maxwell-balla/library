spring:
  application:
    name: library
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://0.0.0.0:5430/library
    username: postgres
    password: postgres

server:
  port: 8087
